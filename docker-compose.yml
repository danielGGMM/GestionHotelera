services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: hoteldb
    ports:
      - "5432:5432"
    volumes:
      - hoteldb_data:/var/lib/postgresql/data

volumes:
  hoteldb_data: